const CertificatesPage = () => {
    return (
        <PageContainer title="Certificate Management">
            <p className="text-gray-600">This feature is a placeholder. You can expand it to generate and manage student certificates.</p>
            <p className="text-gray-600 mt-2">Possible future features include: generating a PDF certificate from a template, storing certificate URLs, and linking them to students.</p>
        </PageContainer>
    );
};
